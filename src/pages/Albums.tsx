import React from 'react'
import Gallery from './Gallery'

// Albums page now uses the Gallery component with Albums functionality
const Albums: React.FC = () => {
  return <Gallery />
}

export default Albums